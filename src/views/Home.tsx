import React from "react";
import {ScrollView, Text} from "react-native";

const Home: React.FC = () => {
    return (
        <ScrollView>
            <Text>Home</Text>
        </ScrollView>
    );
}

export default Home;
