import React from "react";
import {Text} from "react-native";
import Container from "../components/Container";

const CreerRecette: React.FC = () => {
    return (
        <Container showSearchbar={false}>
            <Text>
                CreerRecette
            </Text>
        </Container>
    );
}

export default CreerRecette;